<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior">
    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Your bio"
        android:id="@+id/edit_bio"
        app:layout_constraintBottom_toTopOf="parent"
        app:layout_constraintTop_toTopOf="@id/edit_bio_description"
        />
    <TextView
        app:layout_constraintTop_toBottomOf="@id/edit_bio"
        android:id="@+id/edit_bio_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:text="You can add a few lines about yourself."/>
</androidx.constraintlayout.widget.ConstraintLayout>